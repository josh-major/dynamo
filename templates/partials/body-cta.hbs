<section class="go-to-shop">
  <div class="container">
    <div class="row justify-content-md-between align-items-md-center">
      <div class="col-md-7 col-lg-8 shop-text">
        <h2 class="h2-alt">
          {{{articleBody.body-cta-title}}}
        </h2>
        <p>
          {{{articleBody.body-cta-text}}}
        </p>
      </div>
      <div class="col-md-4 justify-content-end d-flex">
        <a href="{{articleBody.body-cta-link}}" class="btn btn-custom btn-danger">
          {{{articleBody.body-cta-anchor-text}}}
        </a>
      </div>
    </div>
  </div>
</section>
